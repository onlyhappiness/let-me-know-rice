import { alpha } from "@mui/material";

// SETUP COLORS
const PRIMARY = {
  main: "#F97778",
};

const SECONDARY = {
  main: "#F0F4F7",
};

const palette = {
  primary: PRIMARY,
};

export default palette;
